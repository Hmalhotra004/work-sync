"use client";

interface Props {
  taskId: string;
  projectId: string;
  workspaceId: string;
}

const TaskIdView = ({ taskId, projectId, workspaceId }: Props) => {
  return <div>TaskIdView</div>;
};

export default TaskIdView;
